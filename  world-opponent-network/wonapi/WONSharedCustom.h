// Don't put in include guards becuase this file needs to be included twice (because ms string class re-enables some warnings)

// library user can override this file to disable extra warnings, etc... (default is a blank file)
